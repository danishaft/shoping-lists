export interface Input {
    name: string;
    note: string;
    image: string;
    category: string;
  }

  export interface Item {
    id: string
    name: string, 
    image: string,
    note: string,
    category: string
}